#include <scene.h>

/*
Scene::Scene(const char * const filename) {
	animationSpeed = 10;
	currentFrame = 0;

	std::ifstream file;
	file.open(filename);

	while (!file.eof()) {
		std::string line;

		std::getline(file, line);

		if
	}
}
*/
